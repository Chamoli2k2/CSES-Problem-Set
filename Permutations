#include<bits/stdc++.h>
using namespace std;
int main(){
    long long n;
    cin>>n;
    long long arr[n];
    for(long long i=0;i<n;i++){
        cin>>arr[i];
    }

    // base case
    if(n == 1){
        cout<<1<<endl;
    }
    else if(n == 2 || n == 3){
        cout<<"NO SOLUTION"<<endl;
    }

    for(long long i=2;i<=n;i+=2){
        cout<<i<<" ";
    }

    for(int i=1;i<=n;i+=2){
        cout<<i<<" ";
    }

    cout<<endl;
    return 0;
}
